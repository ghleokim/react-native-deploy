export const SCREEN_WIDTH = window.innerWidth || document.body.clientWidth || window.screen.width;
export const SCREEN_HEIGHT = window.innerHeight || document.body.clientHeight || window.screen.height;

export const BREAK_POINT_MOBILE = 768;
export const BREAK_POINT_TABLET = 992;
export const BREAK_POINT_PC = 1200;

export const HEADER_HEIGHT = '80px';

export const config = {
  // HEADER_HEIGHT: 80
}
