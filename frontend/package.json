{
  "private": true,
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "homepage": "https://ghleokim.github.io/react-native-deploy",
  "scripts": {
    "predeploy": "cd packages/common && yarn build && cd ../web && yarn build",
    "deploy": "gh-pages -d frontend/ ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "gh-pages": "^2.2.0"
  }
}
